# Copy to .env and fill in. Never commit .env.

# ── URL / port ──
APP_URL=http://Mac-2.local:3000   # how devices reach the notes app
DOCMOST_PORT=3000                 # app port

# ── Docmost secrets ──
APP_SECRET=openssl-rand-hex-32
DB_PASSWORD=openssl-rand-hex-16
